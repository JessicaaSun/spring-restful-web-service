# move all the configuration to the development profile

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
#jdbc:postgresql://localhost:5432/bank_db

#String serverPath= "src/main/resources/images";
#String clientPath= "/images/**";

server-path = src/main/resources/images/
client-path = /images/**

# change default credentials
#spring.security.user.name=admin
#spring.security.user.password=12345

#Setting up mail server for sending mail to users
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_SERVER}
spring.mail.password=${APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true